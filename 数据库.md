schema_versions / project_metadata => 元数据

files → nodes => 1 文件 : N nodes

edges → nodes => (定义文件和nodes的关系是否为包含关系)

unresolved_refs → nodes => N unresolved_refs : 1 (from_node_id表示来自于nodes表的引用)

nodes → nodes_fts => 1 : 1 | 触发器按 rowid 同步
