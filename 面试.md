1.codeGraph install 安装流程 =>
找到cursor、claude、codex mcp Agent配置文件路径 => 把mcp server的配置写入到各个Agent对应mcp服务的文件路径 => 之后走codeGraph init

2.codeGraph init 初始化流程
<1>.当已经初始化完成 => 基于git hook 触发('post-commit', 'post-merge', 'post-checkout') codeGraph sync
<2>.
2.1当未初始化 scanning(获取所有的文件路径、根据文件推断出使用的框架)
2.2parsing
先通过Work线程加载（解析代码tree-sitter对应WebAssembly包）
对每一个文件 利用tree-sitter 解析 (深度优先遍历)
主线程负责“调度任务”，Worker 负责“解析代码”，两者通过 postMessage 通信，实现一个“异步解析引擎”。

tree-sitter 跑在 WASM 上，线性内存规范上不能shrink，长跑索引易 OOM(out of memory)。我们在 Worker 内 每 5000 次/语言 reset Parser 减碎片，主线程 每 250 文件 recycle Worker 销毁 isolate 彻底释放 WASM 堆，形成 细粒度 + 粗粒度 双层内存策略，提升大仓库索引稳定性。
Parser reset — 每语言每 5000 次 parse(减轻 Parser 长期 parse 带来的 WASM 堆碎片和累积)(不杀线程，只重建 Parser)
Worker recycle — 每 250 个文件(最彻底 — WASM 线性内存整段丢弃，从零开始) (要 terminate、spawn、重新 load-grammars)
2.3 resolving
unresolvedReferences 是索引第一阶段（Tree-sitter 提取）产生的「待解析引用」——代码里明确出现了对另一个符号的引用，但提取器当时还无法确定它指向数据库里的哪个 node。
不断从数据库取未解析引用 → 解析 → 写入图关系 → 删除旧数据 → 直到完成。
2.4 基于git hook 触发('post-commit', 'post-merge', 'post-checkout') codeGraph sync

3.codegraph uninstall 是 CodeGraph 的交互式卸载入口。它负责把安装时写入各 AI 代理（Claude、Cursor、Codex 等）的 MCP 配置和说明块清掉，但不会删除 .codegraph/ 索引目录。

4.codegraph uninit 彻底移除 CodeGraph 的本地索引数据（删除 .codegraph/ 目录），并顺带清理安装时写入的 git hook。

5.codegraph index 相当于 codegraph init。两者都会跑 indexAll() 建索引，但职责不同：init 是「首次接入项目」，index 是「在已有项目上（重新）索引」。

6.codegraph status 用来展示项目有没有初始化、索引有多大、库里有多少符号、哪些文件还没同步、SQLite 后端是否正常等。

7.codegraph query
<7.1> 有query-text\无query-text 查询结果 => 有query-text走FTS5查询、没有走全量查询
<7.2> 上面找不到再走：LIKE 做模糊匹配，并用 CASE 手动实现一个简易排序算法，让“更像的结果排前面”。
<7.3> 当FTS / LIKE都没结果时，通过编辑距离找“拼写最像”的符号名，再去数据库精确查 node
<7.4> 用 SQL 精确匹配补充 BM25 可能漏掉的“短精确名字”
<7.5> 综合打分 （根据类型加分、根据路径加分、名字匹配加分）
<7.6> 最后严格过滤(根据查询的path、name严格过滤)

8.codegraph files 显示项目文件结构 “SELECT \* FROM files ORDER BY path”

9.codeGraph sync => 手动同步
获取所有的文件路径、根据文件推断出使用的框架 跟原有files库中的file对比(比大小、比最后修改时间)，如果当前文件有改变(当前文件删除，files库对应的文件删除)
(当前文件修改，files库对应的文件修改)(新增,files库对应的文件新增)
针对每一个变动的文件
利用tree-sitter 解析 (深度优先遍历)
主线程负责“调度任务”，Worker 负责“解析代码”，两者通过 message 通信 + Promise 映射，实现一个“异步解析引擎”。

tree-sitter 跑在 WASM 上，线性内存规范上不能shrink，长跑索引易 OOM(out of memory)。我们在 Worker 内 每 5000 次/语言 reset Parser 减碎片，主线程 每 250 文件 recycle Worker 销毁 isolate 彻底释放 WASM 堆，形成 细粒度 + 粗粒度 双层内存策略，提升大仓库索引稳定性。
Parser reset — 每语言每 5000 次 parse(减轻 Parser 长期 parse 带来的 WASM 堆碎片和累积)(不杀线程，只重建 Parser)
Worker recycle — 每 250 个文件(最彻底 — WASM 线性内存整段丢弃，从零开始) (要 terminate、spawn、重新 load-grammars)

10.codegraph unlock 删除(如有) .codegraph/codegraph.lock

11.codegraph serve
Direct 模式：单进程直连 stdio，适用于用户显式 opt-out、项目未初始化或 Daemon 不可用时的兜底路径，保证 MCP 会话始终可启动

Proxy 模式：轻量 stdio↔Socket 代理层，作为 IDE/Agent 与后台 Daemon 之间的通信桥梁，内置 PPID 看门狗，宿主进程异常退出时可及时回收代理进程
Daemon 模式：Detached 后台守护进程，多客户端共享同一 CodeGraph 引擎、文件监听与 SQLite 连接，通过 Unix Domain Socket / Windows Named Pipe 对外服务，降低重复冷启动与内存占用
流程
Cursor / Claude（MCP Host）
↕ stdio（标准输入输出，JSON-RPC）
Proxy 进程（轻量转发层）
↕ Unix Socket / Named Pipe
Daemon 进程（真正干活的共享服务）
↕
CodeGraph 引擎 + SQLite + 文件监听
